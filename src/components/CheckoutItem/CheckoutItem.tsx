import type React from "react";

const CheckoutItem: React.FC = () => {
  return (
    <div>
      <h1>Checkout Item Details</h1>
      <p>
        The checkout item details will be displayed here and will lead to
        payment page.
      </p>
    </div>
  );
};

export default CheckoutItem;
